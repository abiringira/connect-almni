<!--
including header content which is common for all pages
-->
<%- include ../layouts/header.ejs %>

    <% if (messages.error) { %>
        <div id="toast" class="toast toast-error">
            <button id="btn-clear" class="btn btn-clear float-right"></button>
            <%- messages.error %>
        </div>
        <% } %>

            <% if (messages.success) { %>
                <div id="toast" class="toast toast-success">
                    <button id="btn-clear" class="btn btn-clear float-right"></button>
                    <%- messages.success %>
                </div>
                <% } %>
                    <div class="content-wrapper--outer padded">
                        <!-- <table width='80%' border=0>
		<tr style='text-align:left; background-color:#CCC'>
			<th>First Name</th>
			<th>Last Name</th>
			<th>Age</th>
			<th>Mobile</th>
			<th>Email</th>
			<th>Action</th>
		</tr>
		<% if (data) { %>
		<% data.forEach(function(user){ %>
			<tr>
				<td><%= user.fname %></td>
				<td><%= user.lname %></td>
				<td><%= user.age %></td>
				<td><%= user.mobile %></td>
				<td><%= user.email %></td>
				
				<td>
					<div style="float:left">
						<a href='/users/edit/<%= user._id %>'>Edit</a> &nbsp;							
						<form method="post" action="/users/delete/<%= user._id %>" style="float:right">
							<input type="submit" name="delete" value='Delete' onClick="return confirm('Are you sure you want to delete?')" />
							<input type="hidden" name="_method" value="DELETE" />
						</form>
					</div>
				</td>
			</tr>
		<% }) %>
		<% } %>
	</table> -->
                        <div class="content-wrapper--inner centerize padded-tb">
                            <div class="heading text-center">
                                <h3>User Informations</h3>
                            </div>
                            <div class="divider"></div>
                            <table class="table table-striped table-hover">
                                <thead>
                                    <tr>
                                        <th>Name</th>
                                        <th>Job</th>
                                        <th>Institution</th>
                                        <th>Phone Number</th>
                                        <th>Faculty</th>
                                        <th>User Details</th>


                                    </tr>
                                </thead>
                                <tbody>
                                    <% if (data) { %>
                                        <% data.forEach(function(user){ %>
                                            <tr>
                                                <td class="text-capitalize">
                                                    <%= user.fullname %>
                                                </td>
                                                <td class="text-capitalize">
                                                    <%= user.job %>
                                                </td>
                                                <td class="text-capitalize">
                                                    <%= user.institution %>
                                                </td>
                                                <td>
                                                    <%= user.phoneNumber %>
                                                </td>
                                                <td class="text-capitalize">
                                                    <%= user.faculty %> </a>
                                                </td>
                                                <td>
                                                    <%= user.message %>
                                                </td>

                                                <td>


                                                    <form method="post" action="/users/sendInfo/<%= user._id %>">
                                                        <div class="wrapper">
                                                            <div class="c1">
                                                                <a class="btn btn-action btn-primary"
                                                                    href='/users/editInformation/<%= user._id %>'><i
                                                                        class="icon icon-edit"></i></a>
                                                            </div>

                                                            &nbsp;&nbsp;
                                                            <div class="c2">

                                                                <button type="submit" name="delete"
                                                                    class="btn btn-action btn-error"
                                                                    onClick="return confirm('Are you sure you want to delete?')"><i
                                                                        class="icon icon-delete"></i></button>
                                                                <input type="hidden" name="_method" value="DELETE" />
                                                            </div>

                                                        </div>
                                                    </form>

                                                </td>



                                            </tr>
                                            <% }) %>
                                                <% } %>
                                </tbody>
                            </table>
                        </div>

                    </div>


                    <!--
including footer content which is common for all pages
-->
                    <%- include ../layouts/footer.ejs %>